# Copie para functions/.env.nosso-momento-app (não versionar).
ENFORCE_APP_CHECK=true

# Opcional: espelhar leads VIP no Google Sheets (somente servidor)
# LP_SHEETS_ENDPOINT=https://script.google.com/macros/s/.../exec
# LP_FORM_TOKEN=

# Meta de vagas exibida no contador
# WAITLIST_META=50
