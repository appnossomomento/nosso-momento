/** Máximo de conexões ativas para usuários VIP (free = 1). */
const MAX_CONEXOES_VIP = 5;

/** Máximo de momentos custom por usuário em um pareamento. */
const MAX_CUSTOM_MOMENTS = 20;

module.exports = {MAX_CONEXOES_VIP, MAX_CUSTOM_MOMENTS};
